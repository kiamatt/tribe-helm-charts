apiVersion: v1
kind: Service
metadata:
  name: {{ include "bootstrap.fullname" . }}
  labels:
    {{- include "bootstrap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bootstrap.selectorLabels" . | nindent 4 }}
